# 第6章 字典与集合


## 6.1 字典（dict）

字典（Dictionary）是 Python 中重要的**映射类型**（Mapping Type），以 **键-值对**（Key-Value Pair）形式存储数据，键必须唯一且不可变（如字符串、数字、元组），值可以是任意类型。

- **定义**：用花括号 `{}` 包裹，由逗号分隔的 `键: 值` 对组成。
- **核心特性**：
  1. **键（Key）必须唯一**：如果重复赋值，后面的会覆盖前面的。
  2. **键必须是不可变（Immutable）类型**：如字符串、整数、浮点数、元组（`tuple`）；**列表（`list`）和字典不能作为键**（因为它们是可变的，会引发 `TypeError`）。Key不可重复，重复会对原有数据覆盖
  3. **值（Value）可以是任意类型**：数字、字符串、列表、甚至另一个字典都可以。
  4. **有序性**：Python 3.7+ 开始，字典**保持插入顺序**（遍历时按插入顺序输出）。

```
特点：
    可以容纳多个数据
    可以容纳不同类型的数据
    每一份数据是Key-Value键值对
    可以通过Key获取到Value，Key不可重复（重复会覆盖）
    不支持下标索引
    可以修改（增加或删除更新元素等）
```

### 1、定义与操作

#### (1) 创建字典

```python
# 空字典
empty = {}

# 使用花括号
person = {"name": "Alice", "age": 25, "city": "Beijing"}

# 使用 dict() 构造函数
person2 = dict(name="Bob", age=30, city="Shanghai")
person3 = dict([("name", "Charlie"), ("age", 35)])

# 字典键可以是不可变类型
d = {1: "one", 2: "two", (1, 2): "tuple key"}  # 元组可作为键
# d = {[1,2]: "list"}  # TypeError: unhashable type 'list'
```

#### (2) 基本操作：增删改查

##### (a) 查

| 方式           | 语法                    | 特点                                                         |
| :------------- | :---------------------- | :----------------------------------------------------------- |
| **直接索引**   | `d["key"]`              | 速度快，但**如果键不存在，直接报错 `KeyError`**（开发时需谨慎） |
| **安全获取**   | `d.get("key", default)` | **极力推荐**！键不存在时返回 `None` 或你指定的 `default` 值，不报错 |
| **获取所有键** | `d.keys()`              | 返回一个视图对象（可迭代）                                   |
| **获取所有值** | `d.values()`            | 返回一个视图对象                                             |
| **获取键值对** | `d.items()`             | 返回 `(key, value)` 元组的视图（遍历常用）                   |

##### (b) 增 & 改

**语法**：`d[key] = value`

- 如果 `key` **不存在** → 新增一对键值。
- 如果 `key` **已存在** → 覆盖（修改）原来的值。

##### (c) 删

| 方式             | 语法                   | 特点                                                        |
| :--------------- | :--------------------- | :---------------------------------------------------------- |
| **删除指定键**   | `del d["key"]`         | 键不存在会报错 `KeyError`                                   |
| **弹出并返回**   | `value = d.pop("key")` | 删除并返回该键的值；可设默认值防报错（如 `pop("x", None)`） |
| **弹出最后一项** | `d.popitem()`          | Python 3.7+ 删除并返回**最后插入**的一个键值对（元组形式）  |
| **清空整个字典** | `d.clear()`            | 清空所有内容，剩下 `{}`                                     |

```python
person = {"name": "Alice", "age": 25}

# 访问值（通过键）
print(person["name"])      # "Alice"
# print(person["gender"])  # KeyError，键不存在会报错

# 添加或修改键值对
person["city"] = "Beijing"   # 新增
person["age"] = 26           # 修改
print(person)   # {"name": "Alice", "age": 26, "city": "Beijing"}

# 删除键值对
del person["city"]
print(person)   # {"name": "Alice", "age": 26}

# pop() 删除并返回对应值
age = person.pop("age")
print(age)      # 26
print(person)   # {"name": "Alice"}

# popitem() 删除并返回最后一个键值对（Python 3.7+ 有序）
person = {"a": 1, "b": 2, "c": 3}
item = person.popitem()
print(item)     # ("c", 3)
print(person)   # {"a": 1, "b": 2}

# clear() 清空
person.clear()
print(person)   # {}
```

#### (3) 遍历字典

```python
person = {"name": "Alice", "age": 25, "city": "Beijing"}

# 遍历键
for key in person:
    print(key, person[key])

# 遍历键值对（推荐）
for key, value in person.items():
    print(f"{key}: {value}")

# 仅遍历值
for value in person.values():
    print(value)
```



#### (4) 字典高频操作

##### keys() / values() / items()

这些方法返回**视图对象**（View），动态反映字典变化，可转换为列表。

```python
person = {"name": "Alice", "age": 25, "city": "Beijing"}

# 获取所有键
keys = person.keys()
print(keys)           # dict_keys(['name', 'age', 'city'])
print(list(keys))     # ['name', 'age', 'city']

# 获取所有值
values = person.values()
print(values)         # dict_values(['Alice', 25, 'Beijing'])
print(list(values))   # ['Alice', 25, 'Beijing']

# 获取所有键值对（元组列表）
items = person.items()
print(items)          # dict_items([('name', 'Alice'), ('age', 25), ('city', 'Beijing')])
print(list(items))    # [('name', 'Alice'), ('age', 25), ('city', 'Beijing')]

# 视图动态反映字典变化
keys = person.keys()
person["gender"] = "female"
print(keys)           # dict_keys(['name', 'age', 'city', 'gender'])
```

##### get() / setdefault() / update()

```python
person = {"name": "Alice", "age": 25}

# get(key, default)：安全获取，不存在返回默认值
print(person.get("name"))           # "Alice"
print(person.get("gender"))         # None
print(person.get("gender", "未知"))  # "未知"

# setdefault(key, default)：若键存在返回对应值，否则设置默认值并返回
person.setdefault("city", "Beijing")
print(person)          # {"name": "Alice", "age": 25, "city": "Beijing"}

person.setdefault("age", 30)   # 键已存在，返回25但不修改
print(person["age"])   # 25

# update()：合并另一个字典或键值对序列
person.update({"job": "Engineer", "age": 26})  # 更新age，新增job
print(person)   # {"name": "Alice", "age": 26, "city": "Beijing", "job": "Engineer"}

# update 也可以接受键值对列表
person.update([("salary", 8000), ("level", "senior")])
print(person)
```


### 2、字典推导式

字典推导式可以从可迭代对象快速生成字典， 是**列表推导式**的“亲兄弟”，语法几乎一样，但它的产物是字典（用花括号 `{}` 包裹），且必须同时生成**键（Key）**和**值（Value）**

####  核心语法

- **列表推导式**：`[表达式 for 变量 in 可迭代对象]` → 生成列表
- **字典推导式**：`{键表达式: 值表达式 for 变量 in 可迭代对象}` → 生成字典

**关键区别**：字典推导式最前面必须是一个 **`键: 值` 对**，中间用冒号 `:` 隔开。

```python
# 基本语法：{key_expr: value_expr for item in iterable if condition}

# 生成平方映射
squares = {x: x**2 for x in range(5)}
print(squares)   # {0: 0, 1: 1, 2: 4, 3: 9, 4: 16}

# 带条件过滤（只保留偶数）
even_squares = {x: x**2 for x in range(10) if x % 2 == 0}
print(even_squares)   # {0: 0, 2: 4, 4: 16, 6: 36, 8: 64}

# 交换键值
original = {"a": 1, "b": 2, "c": 3}
swapped = {v: k for k, v in original.items()}
print(swapped)   # {1: 'a', 2: 'b', 3: 'c'}

# 从两个列表创建字典
keys = ["name", "age", "city"]
values = ["Alice", 25, "Beijing"]
person = {k: v for k, v in zip(keys, values)}
print(person)   # {"name": "Alice", "age": 25, "city": "Beijing"}
# 进阶：可以直接用 dict(zip(keys, values))

# 嵌套推导式（处理矩阵）
matrix = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
row_sum = {f"row{i}": sum(row) for i, row in enumerate(matrix)}
print(row_sum)   # {"row0": 6, "row1": 15, "row2": 24}
```



### 3、字典的底层实现（哈希表）

#### 哈希表原理简述

字典通过**哈希表**（Hash Table）实现，核心思想：
- 对键调用 `hash()` 函数计算哈希值（整数）
- 哈希值对表大小取模，确定存储位置（桶）
- 解决哈希冲突：Python 采用**开放寻址法**（Open Addressing）

#### (a) 哈希值（Hash Value）是什么？

当往字典里放 `{"name": "Alice"}` 时，Python 不会直接存字符串 `"name"`，而是用哈希函数把键变成一个巨大的整数（哈希值）

- 调用 `hash("name")` 得到一个整数（如 `-9191918412397823`）
- **唯一硬性要求**：在程序运行期间，这个键的哈希值**绝不能变**。所以键必须是不可变对象（字符串、整数、元组），列表不行

#### (b) hash核心设计：分离的两张表

传统的哈希表像一个“大杂烩”数组，每个槽位直接存键值对。但 Python 为了节省内存并记住插入顺序，把字典拆成了两块：

##### ① `indices`（索引表）

- 本质是一个**一维整数数组**（里面的数字叫“索引号”）
- 它的长度是**2 的幂次方**（如 8、16、32），初始较小

##### ② `entries`（实体表）

- 本质是一个**按插入顺序排列的数组**，每个元素是一个结构体，包含三样东西：`哈希值（hash）`、`键（key）`、`值（value）`
- **关键特性**：新数据永远 **`append`** 到 `entries` 的末尾，所以 `entries` 的顺序天然就是插入顺序（这是字典可迭代时有顺序的根本原因）

#### (c) 写入数据（`person["gender"] = "female"`）的完整步骤

**Step 1：算哈希 & 取模**

>   计算 `hash("gender")`，得到一个大整数。
>   用这个整数对 `indices` 的长度取模（或位运算 `&`），得到一个**槽位索引**，比如算出来是 `3`。

**Step 2：查看 `indices[3]`**

- 如果 `indices[3]` 是 `-1`（空标志），说明这个位置没人占。
  - 把当前键值对打包，追加到 `entries` 的末尾（假设此时是第 0 个位置）。
  - 把 `0` 这个位置号写入 `indices[3]`。

**Step 3：如果发生“哈希冲突”（槽位被占了）**

> 比如另一个键算出来也是槽位 `3`（虽然概率低，但会发生）。

- Python 不会简单地在 `3` 号槽位往后挪（线性探测），而是采用**伪随机探测（Perturb 算法）**，寻找下一个空闲槽位（比如 `5`），直到找到空位。
- 然后在 `indices[5]` 里存入该键在 `entries` 中的位置号。

> 那如何判断到底是哪个键呢？
> 查找到某个槽位时，Python 会通过 `entries[pos]` 里存的**真实键对象**，与你查找的键进行 **`==` 比较**。只有哈希值相等 **且** 键相等，才算找到；否则继续探测。

#### (d) 读取数据（`person.get("name")`）的查找过程

查找是写入的逆过程：

1. 调用 `hash("name")` → 取模 → 得到槽位 `i`
2. 看 `indices[i]` 的值，得到在 `entries` 中的位置 `pos`
3. 检查 `entries[pos]` 里的键是否等于 `"name"`
   - 是 → 返回 `entries[pos]` 里的值
   - 否 → 按照探测算法找下一个槽位，继续比对，直到找到或遇到空槽位（返回 `None`）

#### (e) 扩容（Rehashing）

当字典里的**元素数量**超过 `indices` 数组长度的 **2/3（负载因子 0.66）** 时，冲突会急剧增加，查询会变慢。此时 Python 会：

1. **把 `indices` 数组长度翻倍**（比如从 8 变成 16）
2. **完全清空 `indices`**
3. **遍历旧的 `entries` 列表**，把里面的每个键**重新计算哈希**，并取模（因为数组长度变了，取模结果变了），**重新插入**到新的 `indices` 中

> 这个过程叫做 **“重新哈希（Rehashing）”**，非常耗时，但为了日常查询的极速，这是必须付出的代价。

#### (f) 为什么字典的键必须不可变？

如果键是列表（可变），列表内容变了，它的 `hash()` 值也会变。
假设你把 `[1, 2]` 作为键存入槽位 `3`，后来你改成 `[1, 2, 3]`，哈希值变了。

- 下次你拿着新列表去查找，取模结果不再是 `3`，Python 直接去别的位置找，**永远找不到**这个键，导致内存泄漏（僵尸数据）。因此 Python 强制要求键必须是可哈希（Hashable）且不可变的。



#### 哈希与不可变要求

- 键必须是**可哈希**（Hashable）的，即对象在其生命周期内哈希值不变，且支持 `__hash__()` 和 `__eq__()` 方法。
- 不可变类型（`int`, `str`, `tuple`）可哈希；可变类型（`list`, `dict`, `set`）不可哈希。

```python
# 查看哈希值
print(hash("hello"))   # 某个整数
print(hash((1, 2)))    # 可哈希

# 自定义类的实例默认可哈希（id 作为哈希）
class MyClass:
    pass
obj = MyClass()
d = {obj: "value"}   # 可行，但通常不推荐

# 但如果类定义了 __eq__ 而未定义 __hash__，则实例不可哈希
class Unhashable:
    def __eq__(self, other):
        return True
# u = Unhashable()
# d = {u: "test"}  # TypeError: unhashable type
```



### 4、字典的查找效率

- 平均时间复杂度：**O(1)**
- 最坏情况（大量哈希冲突）：O(n)，但 Python 通过随机化哈希种子和冲突处理减轻风险。

```python
import time

# 比较列表和字典的查找性能（演示）
big_dict = {i: i for i in range(1000000)}
big_list = list(range(1000000))

# 字典查找 O(1)
start = time.perf_counter()
_ = big_dict[999999]
print("字典查找耗时:", time.perf_counter() - start)

# 列表查找 O(n)（最坏情况）
start = time.perf_counter()
_ = big_list.index(999999)  # 实际是O(n)
print("列表查找耗时:", time.perf_counter() - start)
```

![image-20260825071043067](images/image-20260825071043067.png)

## 6.2 集合（set）

### 1、创建集合

集合（Set）是无序、不重复的元素集合，基于哈希表实现，支持数学集合运算。元素必须是**可哈希**的。

```python
# 空集合: 使用花括号 {} 或内置函数 set() 来定义
empty = set()
# 注意：不能使用 {} 定义空集合，因为 {} 在 Python 中被解析为空字典（dict）。定义空集合必须使用 set()。

# 使用花括号
fruits = {"apple", "banana", "orange"}

# 从列表去重
unique = set([1, 2, 2, 3, 3, 4])
print(unique)   # {1, 2, 3, 4}

# 字符串拆分为字符集合
chars = set("hello")
print(chars)    # {'h', 'e', 'l', 'o'}（无序）

# 注意元素限制：集合中的元素必须是不可变类型（如整数、浮点数、字符串、元组），因为需要哈希值。列表、字典和集合本身不能作为集合的元素（但 frozenset 可以）
# s = {[1,2]}   # TypeError，列表值可变，不能作为集合元素
```

### 2、基本操作：增删改查

##### 1. 查（查询/访问）

集合**无序且无索引**，因此不能通过 `s[0]` 访问元素。查询主要通过**成员检测**和**遍历**实现

#### 2. 增（添加元素）

| 方法                   | 作用                                                         | 特性                                     |
| :--------------------- | :----------------------------------------------------------- | :--------------------------------------- |
| **`add(x)`**           | 添加**单个**元素 `x`                                         | 若已存在，则无任何效果                   |
| **`update(iterable)`** | 批量添加，参数为可迭代对象（列表、元组、字符串、另一个集合等） | 自动拆解元素并去重合并，等价于“并集赋值” |

#### 3. 删（移除元素）

删除时**务必注意**元素不存在时的报错差异：

| 方法             | 作用                       | 元素不存在时                 |
| :--------------- | :------------------------- | :--------------------------- |
| **`remove(x)`**  | 移除指定元素 `x`           | **报错**（抛出 `KeyError`）  |
| **`discard(x)`** | 移除指定元素 `x`           | **不报错**（静默忽略）       |
| **`pop()`**      | 随机删除并**返回**一个元素 | 集合为空时报 `KeyError`      |
| **`clear()`**    | 清空集合内所有元素         | 无返回值，集合变为空 `set()` |

#### 4. 改（更新/修改元素）

由于集合元素不可变（如整数、字符串、元组），**无法直接“原地修改”**一个元素的值（例如把 `1` 改成 `10`）。标准的“改”是以下两种逻辑：

###### **逻辑一：替换（先删后加）**

```python
# 替换（先删后加）**——修改某个已知元素。
s = {1, 2, 3}
if 1 in s:
    s.remove(1)
    s.add(10)   # 结果 {10, 2, 3}
```

###### **逻辑二：批量更新（并集操作）**

-   使用 `update()`（见上文）。
-   使用 `|=` 运算符（Python 3.9+）

```python
# 批量更新（并集操作）——将另一个集合合并进来，相当于“整体更新”。
s = {1, 2}
s |= {2, 3, 4}   # 等价于 s.update({2, 3, 4})，结果 {1, 2, 3, 4}
```

###### **逻辑三：交集/差集更新（保留或剔除）**

```python
# 交集/差集更新（保留或剔除）——保留满足条件的元素
s = {1, 2, 3, 4}
s.intersection_update({2, 3})  # 只保留交集部分 -> {2, 3}
s.difference_update({3})       # 剔除指定元素 -> {2}
```



```python
## ====示例====

s = {1, 2, 3}

# 添加
s.add(4)
print(s)   # {1, 2, 3, 4}

# 删除（存在则移除，不存在不报错）
s.discard(2)
print(s)   # {1, 3, 4}
s.discard(10)   # 无该元素，执行代码无影响，也不报错

# remove（不存在会报错）
s.remove(3)
print(s)   # {1, 4}
# s.remove(10)   # KeyError

# pop() 随机删除并返回一个元素
popped = s.pop()
print(popped, s)   # 可能输出 1, {4}

# clear()
s.clear()
print(s)   # set()

# 成员测试
s = {1, 2, 3}
print(1 in s)   # True
print(4 not in s)  # True

# 长度
print(len(s))   # 3
```

### 3、遍历集合

```python
set1 = {'aaa','bbb','ccc','bbb','aaa','bbb','xyz',123456,12.369,'bbb','ccc','bbb','aaa'}
# 因为集合是无序的，所以集合不支持下标索引访问，因此遍历只能用for循环
def for_set_func():
    index = 0
    for i in set1:
        print(f'遍历集合：第{index}个元素是：{i}')
        index+=1
for_set_func()
```



### 4、集合运算（交集、并集、差集、对称差）

```python
A = {1, 2, 3, 4}
B = {3, 4, 5, 6}

# 交集（元素同时属于 A 和 B）
print(A & B)           # {3, 4}
print(A.intersection(B))   # {3, 4}

# 并集（元素属于 A 或 B）
print(A | B)           # {1, 2, 3, 4, 5, 6}
print(A.union(B))      # {1, 2, 3, 4, 5, 6}

# 差集（属于 A 但不属于 B）
print(A - B)           # {1, 2}
print(A.difference(B)) # {1, 2}

# 对称差（属于 A 或 B，但不同时属于两者）
print(A ^ B)           # {1, 2, 5, 6}
print(A.symmetric_difference(B))  # {1, 2, 5, 6}

# 判断子集/超集
C = {1, 2}
print(C <= A)   # True，C 是 A 的子集
print(C.issubset(A))  # True
print(A >= C)   # True，A 是 C 的超集
print(A.issuperset(C)) # True

# 判断是否不相交
print(A.isdisjoint({5, 6}))   # True，因为 A 没有 5 或 6
```

### 5、集合推导式

>   基本语法：`{expr for item in iterable if condition}`

```python
# 生成平方集合
squares = {x**2 for x in range(10)}
print(squares)   # {0, 1, 4, 9, 16, 25, 36, 49, 64, 81}

# 过滤奇数
odd_squares = {x**2 for x in range(10) if x % 2 == 1}
print(odd_squares)   # {1, 9, 25, 49, 81}

# 提取字符串中唯一字符
text = "hello world"
unique_chars = {c for c in text if c != ' '}
print(unique_chars)   # {'l', 'o', 'h', 'e', 'r', 'd', 'w'}

# 嵌套推导式（提取矩阵中偶数）
matrix = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
evens = {num for row in matrix for num in row if num % 2 == 0}
print(evens)   # {8, 2, 4, 6}
```

### 6、集合的应用

```python
# 去重
words = ["apple", "banana", "apple", "orange", "banana"]
unique_words = list(set(words))
print(unique_words)   # 无序去重

# 查找两个列表的共同元素
list1 = [1, 2, 3, 4]
list2 = [3, 4, 5, 6]
common = set(list1) & set(list2)
print(common)   # {3, 4}
```


### 补充：frozenset（不可变集合）

`frozenset` 是**不可变集合**，一旦创建不能修改。它是可哈希的，因此可以作为字典的键或集合的元素。

#### 创建 frozenset

```python
# 从可迭代对象创建
fs = frozenset([1, 2, 3, 3, 4])
print(fs)           # frozenset({1, 2, 3, 4})

# 空 frozenset
fs_empty = frozenset()
```

#### 支持的操作

- 支持所有**不修改**集合的操作：`&`, `|`, `-`, `^`, `issubset`, `issuperset`, `isdisjoint`, `len`, `in` 等
- 不支持修改操作：`add`, `remove`, `discard`, `pop`, `clear`

```python
fs1 = frozenset([1, 2, 3])
fs2 = frozenset([3, 4, 5])

print(fs1 & fs2)     # frozenset({3})
print(fs1 | fs2)     # frozenset({1, 2, 3, 4, 5})
print(fs1 - fs2)     # frozenset({1, 2})
print(fs1 ^ fs2)     # frozenset({1, 2, 4, 5})
print(2 in fs1)      # True
print(len(fs1))      # 3

# 不可变集合可作为字典键
d = {frozenset({1, 2}): "value"}
print(d)   # {frozenset({1, 2}): 'value'}


# 遍历
# 普通 for 循环（顺序随机，不保证插入顺序）
fs = frozenset([5, 1, 4, 2, 3])
for item in fs:
    print(item)
# 输出顺序每次可能不同，例如：1, 2, 3, 4, 5 或 4, 2, 5, 1, 3
```

#### 与普通集合的对比

| 特性         | `set`            | `frozenset`      |
| ------------ | ---------------- | ---------------- |
| 可变性       | 可变             | 不可变           |
| 可哈希       | 否（不可作为键） | 是（可作为键）   |
| 支持修改方法 | 是               | 否               |
| 适用场景     | 动态集合         | 常量集合、作为键 |

#### 综合应用示例

```python
# 统计一段文字中出现的单词及其出现次数（使用字典和集合）
text = "apple banana apple orange banana apple"
words = text.split()
word_count = {}
unique_words = set(words)

for word in unique_words:
    word_count[word] = words.count(word)

print(word_count)   # {'banana': 2, 'apple': 3, 'orange': 1}

# 使用 frozenset 作为缓存键（表示参数组合）
cache = {}
def expensive_function(a, b):
    key = frozenset([a, b])   # 参数组合作为键
    if key in cache:
        return cache[key]
    result = a ** b  # 模拟耗时计算
    cache[key] = result
    return result

print(expensive_function(2, 10))  # 计算
print(expensive_function(10, 2))  # 因为 frozenset 相同，命中缓存（但计算错误，因为 a,b 顺序不同但结果不同？此处仅为演示）
# 注意：实际应使用 (a,b) 元组作为键，这里仅为示例 frozenset 用法
```

---

## 本章小结

- **字典**是键值对映射，基于哈希表实现，查找效率 O(1)。键必须不可变。
- 常用方法：`keys()`, `values()`, `items()`, `get()`, `setdefault()`, `update()`。
- **字典推导式**可快速生成字典。
- **集合**是无序、不重复的元素集合，支持数学运算（交并差等）。
- **frozenset** 是不可变集合，可作为字典键。
- 哈希表的理解有助于掌握字典和集合的高效性。

所有代码可保存为 `chapter6_dict_set.py` 并运行测试。